{
  "title": "Rails Girls Slides",
  "slides": [
    {% for i in (1..3) %}
      {% assign slide = 'slide_' | append: i %}
      {% include slide %}{% unless forloop.last %},{% endunless %}
    {% endfor %}
  ]
}